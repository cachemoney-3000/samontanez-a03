/*
 *  UCF COP3330 Fall 2021 Assignment 3 Solutions
 *  Copyright 2021 Joshua Samontanez
 */

/*
Main:
    PROMPT: "Enter a password:"
    'password' will store the user input
    Call the finalOutput to print result.

Method passwordValidator:
    if (password only consists of numbers and contains less than 8 char)
        passStrength = very weak password
    else if(password only consists of letters and fewer than 8 chars)
        passStrength = weak password
    else if(password consists of alphanumeric and at least 8 chars)
        passStrength = strong password
    else
        passStrength = very strong password

Method finalOutput:
    (Will print out the result of passwordValidator)
    Print: The password <password> is a <passStrength> password.

 */

package baseline;

public class Solution25 {
}
